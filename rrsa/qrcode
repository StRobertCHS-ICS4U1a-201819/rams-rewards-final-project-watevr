Python file:

from kivy.garden.qrcode import QRCodeWidget
parent.add_widget(QRCodeWidget(data="Kivy Rocks"))

Kivy file:

#:import QRCodeWidget kivy.garden.qrcode

BoxLayout:
    orientation: 'vertical'
    Button:
        text: 'press to change qrcode'
        on_release: qr.data = "Kivy Rocks!"
    QRCodeWidget:
        id: qr
        data: 'Hello World'

# errors in the process of integrating the code above to my ram rewards student app

# code found online